# Deploy Casa Torino Web

Siempre usar:
```bash
bash scripts/deploy-web.sh
```

Eso ejecuta `assert-light-theme.sh` y evita volver a publicar la web en oscuro.
TPV y Cocina tienen su propio CSS embebido (pueden seguir oscuros).
